# Frontend Language

The frontend of this application MUST use English for all user-facing text strings, labels, placeholders, and error messages.
Do NOT use Vietnamese or any other language for the UI.
